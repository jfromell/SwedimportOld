<% semantic_form_for [:admin, @product.category, @product] do |f| %>
	
	<% f.inputs :name => "Produkt" do %>
		<%= f.input :name, :required => true %>
		<%= f.input :measures, :required => false %>
	<% end %>
	
	<% f.buttons do %>
		<%= f.commit_button %>
	<% end%>
	
<% end %>
