<%= semantic_form_for [:admin, @product.category] do |f| %>
	<% f.inputs do %>
		<%= f.input :name, :label => 'Benämning' %>
		<%= f.input :measures, :label => 'Mått' %>
	<% end %>
	<% f.buttons do %>
		<%= f.submit %>
	<% end %>
<% end %>